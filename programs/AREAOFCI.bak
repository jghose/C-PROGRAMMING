										  #include<stdio.h>
										  #include<math.h>
										  int main()
										  {
										  double pie=3.1416,r,result,c;
										  printf("enter the value of r: ");
										  scanf("%lf",&r);

										  r=pow(r,2);
										  c=pie*r;
										  printf("the value of r is: %lf", c);
										  return 0;
										  }