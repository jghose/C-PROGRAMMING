										  #include<stdio.h>
										  #include<math.h>
										  int main()
										  {
										  double pie=3.1416,r,result,c;
										  printf("enter the value of r");
										  scanf("%lf",r);
										  c=pie*r
										  r=result;
										  result=pow(r,2);
										  printf("the value of r is: %lf",r);
										  return 0;
										  }